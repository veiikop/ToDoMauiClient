<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ToDoMauiClient.ViewModels"
             x:Class="ToDoMauiClient.Views.ProfilePage"
             Title="Профиль">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
            
            <HorizontalStackLayout HorizontalOptions="End">
                <Button Text="Назад в списки"
                    Command="{Binding GoBackToListsCommand}"
                    BackgroundColor="Transparent"
                    TextColor="Blue" />
            </HorizontalStackLayout>
            
            <Label Text="Ваш профиль" FontSize="32" HorizontalOptions="Center" FontAttributes="Bold" />

            <Label Text="{Binding User.Username, StringFormat='Имя: {0}'}" FontSize="18" />
            <Label Text="{Binding User.Email, StringFormat='Email: {0}'}" FontSize="18" />
            <Label Text="{Binding User.Role, StringFormat='Роль: {0}'}" FontSize="18" />

            <Button Text="Обновить" Command="{Binding RefreshCommand}" />
            <Button Text="Выйти" Command="{Binding LogoutCommand}" TextColor="Red" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="Blue" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>